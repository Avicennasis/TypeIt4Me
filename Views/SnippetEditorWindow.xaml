<Window x:Class="TypeIt4Me.Views.SnippetEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TypeIt4Me.ViewModels"
        mc:Ignorable="d"
        Title="Snippet Editor" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name -->
        <TextBlock Text="Name" Grid.Row="0"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1"/>

        <!-- Category & Hotkey -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="Category (Optional)"/>
                <TextBox Text="{Binding Category}"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Hotkey"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="HotkeyBox" 
                             PreviewKeyDown="HotkeyBox_PreviewKeyDown" 
                             IsReadOnly="True"
                             Text="{Binding TriggerKey, StringFormat='Key: {0}'}" 
                             d:Text="Ctrl+Alt+T"/>
                    <Button Content="X" Command="{Binding ClearHotkeyCommand}" Grid.Column="1" Width="20" Height="20" Margin="5,0,0,10" VerticalAlignment="Top"/>
                </Grid>
                <TextBlock Text="{Binding TriggerModifiers, StringFormat='Modifiers: {0}'}" FontSize="10" Foreground="Gray" Margin="0,-8,0,0"/>

            </StackPanel>
        </Grid>

        <!-- Content -->
        <TextBlock Text="Content" Grid.Row="3"/>
        <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" 
                 Height="150" 
                 AcceptsReturn="True" 
                 AcceptsTab="True" 
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="5" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Height="30" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
